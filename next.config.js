/** @type {import('next').NextConfig} */
const nextConfig = {
  standalone: "export"
};

// next.config.js
module.exports = {
  experimental: {
    appDir: false,
  },
};
module.exports = nextConfig;
